create table notice1 (no number primary key,
title varchar2(200) not null,
nalja date default sysdate not null,
count number default 0 not null,
content varchar2(4000)
);

comment on table notice1 is '공지사항';

create sequence notice1_seq start with 1 increment by 1;


===========================menu===========================
create table menu1(no number primary key, 
title varchar2(100) not null, 
content varchar2(4000) not null, 
kcal number, 
sodium number, 
fat number, 
saccharide number, 
protein number, 
caffeine number, 
originalfilename varchar2(100), 
newfilename varchar2(100),
nalja date default sysdate not null,
category varchar2(20) not null
);

comment on table menu is '커피 음료 음식메뉴';

create sequence menu1_seq start with 1 increment by 1;